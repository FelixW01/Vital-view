<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chart</title>
    <link rel="stylesheet" href="css/styles.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css"
    />
  </head>
  <body>
    {{>header}}


      
       

        <section class="info-section">
          <h2 class="topTitle">Diabetes Tracker</h2>
        </section>

<div class="input-chart-container">
        <section class="input-section">
          <label for="bloodSugar">Enter your blood sugar level (mg/dL): </label>
          <input 
            type="number"
            id="bloodSugar"
            placeholder="Blood Sugar"
            min="50"
            max="500"
          />
          <button class="addDataBtn" onclick="updateChart()">Add Data</button>
        </section>
<section class="container">
        <!-- Chart Section -->
        <section class="chart-section">
          <h2>Your Blood Sugar Levels</h2>
          <canvas id="bloodSugarChart"></canvas>
        </section>
   </div>

<section>
 <h2 class="topTitle">Food Recommendations</h2>
<div class="recommendations-container">
  <section class="search-form-section">
  <form id="recipeSearchForm" action="/chart" method="GET">
    <label for="searchInput">Search for recipes:</label>
    <input type="text" id="searchInput" name="q" placeholder="Example: Chicken">
    <button type="submit">Search</button>
    <div class="sticky-text">
    <h3>Diabetes in the USA</h3>
<p>Over 34 million Americans have diabetes, and another 88 million have prediabetes. Diabetes is a chronic condition that can affect many aspects of your life.</p><p>Good nutrition not only controls glucose (blood sugar) levels, but also improves cholesterol and blood pressure.</p></div>
  </form>

 </section>
  <div id="foodRecommendations">
  {{#if food.length}}
    {{#each food}}
      {{#if this.label}}
      <div class="food-item">
        <img src="{{image}}" alt="{{label}}">
        <div class="food-info">
          <h3>{{label}}</h3>
          <div class="food-details">
        <p>Source: {{source}}</p>
        <p>Calories: {{calories}} kcal</p>
        <p>Sugar: {{sugar}} g</p>
        <div class="nutrition-info">
          <p><a href="{{url}}" target="_blank" class="view-recipe-btn">Recipe</a></p>
        </div>
        <button class="saveFood" data-id="{{this.id}}" data-label="{{label}}" data-source="{{source}}" data-image="{{image}}" data-url="{{url}}" data-calories="{{calories}}" data-sugar="{{sugar}}">
          Save Selection
        </button>
      </div>
    </div>
    </div>
      {{/if}}
    {{/each}}
  {{else}}
    <p>No food recommendations available at the moment.</p>
  {{/if}}
</div>

<a href="/recipeList">Get Saved Recipes</a>

  </section>
{{!-- </div>
</section> --}}
    {{>footer}}

    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chartjs-adapter-date-fns"></script>
    <script src="js/app.js"></script>
  </body>
</html>
